[tool.poetry]
name = "semantix"
version = "0.1.0"
description = ""
authors = ["chandralegend"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
loguru = "^0.7.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
